const projects=window.PROJECT_DATA||[];const cards=[...document.querySelectorAll('.project-card')];const filters=[...document.querySelectorAll('.filter')];const search=document.querySelector('#projectSearch');const count=document.querySelector('#resultCount');const empty=document.querySelector('#emptyState');let current='All';function apply(){const q=search.value.trim().toLowerCase();let visible=0;cards.forEach(card=>{const okCat=current==='All'||card.dataset.category===current;const okSearch=!q||card.dataset.search.includes(q);const show=okCat&&okSearch;card.hidden=!show;if(show)visible++});count.textContent=`Showing ${visible} project${visible===1?'':'s'}`;empty.hidden=visible!==0}filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');current=btn.dataset.filter;apply()}));search.addEventListener('input',apply);const dialog=document.querySelector('#projectDialog');const close=document.querySelector('.dialog-close');function openProject(id){const p=projects.find(x=>x.id===id);if(!p)return;document.querySelector('#dialogImage').src=`assets/images/${p.image}`;document.querySelector('#dialogImage').alt=`Evidence preview for ${p.title}`;document.querySelector('#dialogCategory').textContent=p.category;document.querySelector('#dialogTitle').textContent=p.title;document.querySelector('#dialogOverview').textContent=p.overview;document.querySelector('#dialogHighlights').innerHTML=p.highlights.map(x=>`<li>${x}</li>`).join('');document.querySelector('#dialogResult').textContent=p.result;document.querySelector('#dialogTools').innerHTML=p.tools.map(x=>`<span>${x}</span>`).join('');const doc=document.querySelector('#dialogDoc');doc.href=`assets/docs/${p.doc}`;dialog.showModal()}document.addEventListener('click',e=>{const trigger=e.target.closest('[data-open]');if(trigger)openProject(trigger.dataset.open)});close.addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});const theme=document.querySelector('#themeToggle');const saved=localStorage.getItem('portfolio-theme');if(saved==='light')document.body.classList.add('light');theme.addEventListener('click',()=>{document.body.classList.toggle('light');localStorage.setItem('portfolio-theme',document.body.classList.contains('light')?'light':'dark')});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){const el=entry.target;const target=Number(el.dataset.count);let n=0;const step=Math.max(1,Math.ceil(target/18));const timer=setInterval(()=>{n=Math.min(target,n+step);el.textContent=n;if(n===target)clearInterval(timer)},55);observer.unobserve(el)}}),{threshold:.6});document.querySelectorAll('[data-count]').forEach(el=>observer.observe(el));